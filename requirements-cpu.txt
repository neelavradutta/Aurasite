# CPU install (no NVIDIA GPU): pip install -r requirements-cpu.txt
# GPU install (NVIDIA):         pip install -r requirements-gpu.txt
#
# The service auto-uses GPU when CUDA is available, otherwise CPU.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0
numpy>=1.26.4,<2.0
opencv-python-headless==4.10.0.84
pillow==11.1.0
httpx==0.28.1
scipy==1.15.0
ffmpeg-python==0.2.0
imageio==2.36.1
imageio-ffmpeg==0.5.1
yt-dlp>=2024.12.0
ultralytics>=8.3.0
paddleocr==2.9.1

--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# Pinned for Windows CPU stability — avoid paddle 3.x oneDNN bug
paddlepaddle==2.6.2
