# ANPR AI Service — full Python stack (install from repo root)
#   python -m venv ai-service/.venv
#   ai-service/.venv/Scripts/pip install -r requirements.txt   (Windows)
#   ai-service/.venv/bin/pip install -r requirements.txt       (Linux/macOS)
#
# GPU PyTorch: replace the extra-index-url below with
#   https://download.pytorch.org/whl/cu121

--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0
numpy>=1.26.4,<2.0
opencv-python-headless==4.10.0.84
pillow==11.1.0
httpx==0.28.1
scipy==1.15.0
ffmpeg-python==0.2.0
imageio==2.36.1
imageio-ffmpeg==0.5.1
yt-dlp>=2024.12.0

# PyTorch (CPU wheels via extra-index-url above)
torch>=2.0.0
torchvision>=0.15.0

# ML stack (pinned for Windows CPU stability — avoid paddle 3.x oneDNN bug)
ultralytics>=8.3.0
paddlepaddle==2.6.2
paddleocr==2.9.1
